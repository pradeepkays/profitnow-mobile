// import * as Sentry from '@sentry/react-native'

// if (Config.SENTRY_ENABLED === 'true') {
//   Sentry.init({
//     dsn: Config.SENTRY_DNS,
//   })
// }

// export function sentryAddBreadcrumb(params: Sentry.Breadcrumb) {
//   if (Config.SENTRY_ENABLED === 'true') {
//     Sentry.addBreadcrumb({ level: 'info', ...params })
//   }
// }
